this is new post page

<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.collection_select :user_id, @users, :id, :name %>
  <%= f.submit 'create a new post' %>
<% end %>

<%= link_to 'go back', posts_path %>
